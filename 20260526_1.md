# 流式 VAD Memory 方法对比

## 实验设置

任务：流式视频异常检测（Streaming Video Anomaly Detection, Streaming VAD）。

测试设置：在 NWPU 流式 VAD 场景下，对具备完整历史上下文的样本进行评估。

模型：Qwen3VL-8B。

说明：本文档中的 `FluxMem-like` 和 `FreshMem-style` 均为基于论文思想实现的 Qwen3VL proxy，用于比较不同 streaming memory 思路；它们不是官方代码复现结果。

---

## 方法思路

### 1. Qwen3VL FluxMem-like visual-memory proxy

FluxMem 的核心思想是 **adaptive hierarchical memory**。

它不是把所有历史帧都保留下来，而是把流式视频中的历史视觉信息组织成层次化 memory：

```text
视频帧
→ visual tokens
→ short-term memory 保留最新视觉信息
→ mid-term memory 删除时间冗余
→ long-term memory 合并空间冗余
→ 用压缩后的 visual memory 辅助当前视频理解
```

FluxMem 的重点是：

```text
在长视频/流视频中压缩冗余视觉信息，
降低显存和计算开销，
同时尽量保留对当前推理有用的历史视觉内容。
```

在本实验中，我们实现了一个 Qwen3VL 版本的 FluxMem-like visual-memory proxy：

```text
当前 clip
+ 历史 chunks 中压缩选择出的少量视觉帧
→ Qwen3VL 判断当前 clip 是否异常
```

这个 proxy 主要借鉴 FluxMem 的两点：

1. **短期记忆保留最近视觉信息**：近期历史片段更可能和当前状态连续，因此保留较多视觉细节。
2. **中长期记忆做视觉压缩**：较早历史片段不全部保留，只保留少量代表帧，近似 long-term visual memory。

它回答的问题是：

> 如果只做流式视觉 memory 压缩，是否足够提升 VAD？

---

### 2. FreshMem-style visual-memory proxy

FreshMem 的核心思想是 **frequency-space hybrid memory**。

它认为流式视频 memory 需要同时处理两个问题：

```text
近期历史需要高保真，因为短期动作细节重要；
远期历史需要压缩成全局 gist，因为完整保留会导致上下文过长。
```

FreshMem 主要包含两个模块：

```text
Multi-scale Frequency Memory, MFM:
将远期历史压缩成频率域 gist，保留长期趋势和全局语义。

Space Thumbnail Memory, STM:
将连续视频流切分成 episode，并压缩成空间缩略图 memory。
```

可以概括为：

```text
远期历史 → 全局 gist / 缩略 memory
近期历史 → 高保真 short-term memory
当前 clip + hybrid memory → 当前视频理解
```

在本实验中，我们实现了一个 FreshMem-style visual-memory proxy：

```text
较早历史 chunk → 压缩成 long-term thumbnail frames
最近历史 chunk → 保留更多 short-term high-fidelity frames
当前 clip + FreshMem-style memory
→ Qwen3VL 判断当前 clip 是否异常
```

这个 proxy 主要借鉴 FreshMem 的两点：

1. **近细远粗**：最近历史保留更多细节，远期历史只保留压缩概览。
2. **空间缩略记忆**：远期历史不作为完整视频输入，而是转成少量代表性视觉 memory。

它回答的问题是：

> 如果采用“近期高保真 + 远期 gist”的 memory 组织方式，是否比普通层次化压缩更适合流式 VAD？

---

### 3. 我的 VAD 方法

我的方法借鉴了 FluxMem 和 FreshMem 的 streaming memory 思想，但核心目标不同。

FluxMem 和 FreshMem 主要关注：

```text
如何压缩历史 memory；
如何在长视频中保留足够多的历史信息；
如何降低显存和计算开销。
```

我的方法关注的是：

```text
哪些历史 memory 会帮助 VAD；
哪些历史 memory 会误导 VAD；
哪些 memory 应该保留、抑制、隔离或遗忘。
```

因此，我的方法不是简单地保留更多历史，而是做 **VAD-oriented Memory Governor**。

整体流程是：

```text
当前 clip
→ 分析当前异常风险、场景线索、对象/行为变化
→ 从历史 memory 中选择支持当前异常判断的 evidence
→ 抑制会把异常解释成正常的 normal memory
→ 隔离和当前场景冲突的 misleading memory
→ 用当前 clip + 筛选后的 memory 判断是否异常
```

具体来说，我的方法借鉴 FluxMem 的层次化 memory 组织：

```text
short-term memory:
保留最近历史中和当前事件连续的证据。

mid-term memory:
保留行为变化、对象关系变化、风险上升等中期事件线索。

long-term memory:
保留场景和行为原型，但只作为先验校准，不能压制当前异常证据。
```

同时借鉴 FreshMem 的近细远粗思想：

```text
近期历史:
保留更多细节，用于判断动作连续性和异常前兆。

远期历史:
压缩成场景/行为 gist，只用于理解背景，不直接作为当前正常的证据。
```

在此基础上，我的方法增加了 VAD 特有的 memory 治理机制：

```text
Evidence Memory:
对当前异常判断有帮助的历史证据，例如靠近、追逐、越界、遗留物、风险上升。

Prior Memory:
场景中的常见正常模式，例如道路上行走、广场上正常活动。
它只能用于场景兼容性校准，不能直接降低当前异常风险。

Counter / Quarantine Memory:
表面相似但可能误导当前判断的历史 memory。
例如历史中“骑车在道路上正常”，不能用来解释当前“骑车进入人群/广场/禁区”是正常。
```

我的方法的核心不是：

```text
把历史 memory 压得更小。
```

而是：

```text
让模型知道哪些 memory 应该用于当前 VAD 判断，
哪些 memory 可能会把异常正常化，
哪些 memory 应该被抑制或遗忘。
```

一句话概括：

> FluxMem-like 和 FreshMem-style 主要解决 memory compression；我的方法进一步解决 memory validity，即历史 memory 是否适合作为当前异常判断的证据。

---

## VAD Accuracy

| 方法 | VAD Accuracy |
|---|---:|
| Qwen3VL FluxMem-like visual-memory proxy | 63.8% |
| FreshMem-style visual-memory proxy | 66.0% |
| 我的 VAD 方法 | **85.1%** |

---

## 当前结论

在当前 NWPU 流式 VAD 设置下，FreshMem-style proxy 略高于 FluxMem-like proxy，说明“近期高保真 + 远期 gist”的 memory 组织方式比普通视觉 memory 压缩更适合当前测试口径。

我的 VAD 方法进一步提升到 85.1%，说明对于异常检测任务，仅仅压缩和保留历史 memory 还不够；更关键的是判断历史 memory 是否会误导当前异常检测，并对 memory 进行选择性使用、抑制和遗忘。

---

## 参考文献

1. FluxMem: Adaptive Hierarchical Memory for Streaming Video Understanding. arXiv:2603.02096.  
   https://arxiv.org/abs/2603.02096

2. FreshMem: Brain-Inspired Frequency-Space Hybrid Memory for Streaming Video Understanding. arXiv:2602.01683.  
   https://arxiv.org/abs/2602.01683
